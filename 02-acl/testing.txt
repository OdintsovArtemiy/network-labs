Тестирование ACL:

1. PC из VLAN 10 (192.168.10.5) → сервер 192.168.1.10 → ДОСТУП ЕСТЬ
2. PC из VLAN 20 (192.168.20.5) → сервер 192.168.1.10 → ДОСТУП ЗАПРЕЩЁН (timeout)

Проверка через show access-lists:
- Видим счётчик совпадений (matches) для deny правила
